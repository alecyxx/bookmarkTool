# 安全策略

## 支持范围

安全修复优先针对默认分支上的最新代码。个人部署请及时更新到最新提交，并在升级前完成数据库备份。

## 报告漏洞

请不要在公开 Issue、讨论区或提交信息中发布可利用漏洞、凭据或个人数据。优先使用 GitHub Security Advisories 的私密报告入口；如果仓库尚未启用该入口，请通过维护者提供的私密联系方式报告，并附上影响范围、复现步骤和建议修复方向。

收到报告后，维护者会先确认问题、评估影响，再决定修复版本和公开时间。请给维护者合理的修复窗口，不要在修复前公开披露细节。

## 部署安全提醒

- 不要提交 `.env`、Session 密钥、数据库、备份或真实日志。
- `SESSION_SECRET` 应使用强随机值，且不同环境不能复用。
- 默认只在可信内网监听；公网暴露前必须补充身份认证、TLS、限流和网络访问控制。
- 恢复备份前确认备份来源可信，并在恢复后执行完整性和登录检查。
