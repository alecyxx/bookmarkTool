# 运行依赖（BM-V1-002 精确锁定，Python >=3.11；2026-09 验证于 3.14）
fastapi==0.141.1
uvicorn==0.52.4
jinja2==3.1.6
sqlalchemy==2.0.52
alembic==1.19.2
argon2-cffi==25.1.0
itsdangerous==2.2.0
python-multipart==0.0.32
# IANA 时区数据：zoneinfo 依赖；Windows/精简容器镜像无系统 tzdata 时必须
# 在 config 的 ZoneInfo 校验前完成，见 app/config.py 中 import tzdata 说明
tzdata==2026.3
